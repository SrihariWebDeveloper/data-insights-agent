---
noteId: "0b8a0ac0c88f11f0931777e45d829474"
tags: []

---

# System Architecture & Visual Guide

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER'S BROWSER                              │
│                   (http://localhost:5173)                        │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ HTTP/HTTPS
                           │
                    ┌──────▼────────┐
                    │ React Frontend │
                    │  (Vite Dev)    │
                    └──────┬────────┘
                           │
                           │ REST API (JSON)
                           │
              ┌────────────▼────────────┐
              │  Express API Gateway    │
              │   (Port 3000)           │
              │  - Aggregation          │
              │  - Error Handling       │
              │  - CORS                 │
              └────────────┬────────────┘
                           │
           ┌───────────────┼───────────────┐
           │               │               │
    ┌──────▼────────┐ ┌───▼────────┐ ┌───▼────────┐
    │ Flask Backend │ │   Gemini   │ │ Tools Exec │
    │ (Port 8001)   │ │   Agent    │ │            │
    │               │ │ (Node.js)  │ │            │
    │ - Summary     │ │ - Chat     │ │ - Python   │
    │ - Metrics     │ │ - Insights │ │ - Data Proc│
    │ - Charts      │ │ - Analysis │ │            │
    │ - Chat        │ │            │ │            │
    └───────────────┘ └────────────┘ └────────────┘
```

---

## 📱 Frontend Component Hierarchy

```
App (Main Container)
├── QueryInput Component
│   ├── Header (Gradient Text)
│   ├── Search Form
│   │   ├── Search Icon
│   │   ├── URL Input
│   │   └── Analyze Button
│   └── Example Datasets Info
│
├── Error Display (Conditional)
│   └── Error Message Box
│
├── Loading State (Conditional)
│   ├── Rotating Spinner
│   └── Loading Text
│
└── Dashboard Component (Conditional)
    ├── Dataset URL Display
    │
    ├── SummaryCard Component
    │   ├── Grid (4 columns)
    │   │   ├── StatCard (Rows)
    │   │   ├── StatCard (Columns)
    │   │   ├── StatCard (Missing %)
    │   │   └── StatCard (Types)
    │   │
    │   └── Detailed Info
    │       ├── Column Types List
    │       └── Missing Values List
    │
    ├── MetricsCard Component
    │   ├── Numeric Metrics Grid
    │   │   └── StatCard per column
    │   │
    │   ├── Categorical Metrics Grid
    │   │   └── StatCard per column
    │   │
    │   └── Detailed Statistics
    │       ├── Numeric JSON View
    │       └── Categorical JSON View
    │
    ├── Chat Component (AI Insights)
    │   └── Formatted Text
    │
    └── Chart Component
        └── Base64 Image Display
```

---

## 🔄 Data Flow Diagram

```
User Input
    │
    ▼
┌─────────────────┐
│ QueryInput Form │  (URL of CSV dataset)
└────────┬────────┘
         │
         ▼
    ┌────────────────────┐
    │ Validate URL       │
    │ Show Loading...    │
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────────────────────────┐
    │ POST /api/analyze                      │
    │ { "datasetUrl": "..." }                │
    └────────┬───────────────────────────────┘
             │
             ▼
    ┌────────────────────────────────────────┐
    │ Express API Gateway (api-server.js)    │
    │                                        │
    │ Promise.allSettled([                   │
    │   POST /agent/summary                  │
    │   POST /agent/metrics                  │
    │   POST /agent/chat                     │
    │   POST /agent/chart                    │
    │ ])                                     │
    └────────┬───────────────────────────────┘
             │
    ┌────────┴────────┬──────────┬───────────┐
    │                 │          │           │
    ▼                 ▼          ▼           ▼
┌────────────┐ ┌─────────┐ ┌───────┐ ┌─────────┐
│  Summary   │ │ Metrics │ │ Chart │ │  Chat   │
│  Tool      │ │  Tool   │ │ Tool  │ │  Tool   │
└────────┬───┘ └────┬────┘ └───┬───┘ └────┬────┘
         │          │          │          │
         └──────────┴──────────┴──────────┘
                    │
                    ▼
         Flask Backend (app.py)
         
         Pandas Processing
         
         Returns Results
         
                    │
         ┌──────────┴──────────┬──────────┬──────────┐
         │                     │          │          │
         ▼                     ▼          ▼          ▼
    ┌────────┐          ┌────────┐  ┌────────┐  ┌────────┐
    │Summary │          │Metrics │  │ Chart  │  │ Chat   │
    │Object  │          │Object  │  │Base64  │  │String  │
    └───┬────┘          └───┬────┘  └────┬───┘  └───┬────┘
        │                   │            │          │
        └───────────────────┴────────────┴──────────┘
                            │
                    API Response (JSON)
                            │
                            ▼
                    Express Gateway
                    (Transforms & Merges)
                            │
                            ▼
                    React Frontend
                    (setResults)
                            │
         ┌──────────────────┼──────────────────┐
         │                  │                  │
         ▼                  ▼                  ▼
    ┌─────────┐        ┌──────────┐      ┌──────────┐
    │Summary  │        │ Metrics  │      │Dashboard │
    │Display  │        │ Display  │      │ Display  │
    └─────────┘        └──────────┘      └──────────┘
```

---

## 🎨 UI Component Layout

```
┌──────────────────────────────────────────────────────────┐
│                    DASHBOARD                              │
├──────────────────────────────────────────────────────────┤
│                                                            │
│   ┌────────────────────────────────────────────────────┐  │
│   │          DATA INSIGHTS DASHBOARD                  │  │
│   │    🔍 Enter dataset CSV URL... [Analyze] ✓        │  │
│   └────────────────────────────────────────────────────┘  │
│                                                            │
│   ┌────────────────────────────────────────────────────┐  │
│   │            DATASET OVERVIEW                       │  │
│   ├──────────┬──────────┬──────────┬─────────┤        │  │
│   │  150     │    5     │  0.13%   │   4     │        │  │
│   │  Rows    │ Columns  │ Missing  │ Types   │        │  │
│   ├──────────┴──────────┴──────────┴─────────┤        │  │
│   │                                           │        │  │
│   │ Column Types │ Column Types │             │        │  │
│   │ • col1: int  │ • col1: 0    │             │        │  │
│   │ • col2: str  │ • col2: 0    │             │        │  │
│   │ • col3: flt  │ • col3: 0    │             │        │  │
│   │              │              │             │        │  │
│   └──────────────────────────────────────────┘        │  │
│                                                            │
│   ┌────────────────────────────────────────────────────┐  │
│   │         STATISTICAL METRICS                       │  │
│   ├────────────┬────────────┬────────────┬────────────┤  │
│   │ col1       │ col2       │ col3       │ col4       │  │
│   │ Mean: 5.84 │ Mean: 3.05 │ Unique: 3  │ Mean: 1.20 │  │
│   │ Std: 0.83  │ Std: 0.43  │            │ Std: 0.76  │  │
│   ├────────────┴────────────┴────────────┴────────────┤  │
│   │ Detailed Statistics (JSON Format):                │  │
│   │ { "numeric": {...}, "categorical": {...} }        │  │
│   └────────────────────────────────────────────────────┘  │
│                                                            │
│   ┌────────────────────────────────────────────────────┐  │
│   │             AI INSIGHTS                           │  │
│   │ The dataset contains 150 samples with 4 numeric   │  │
│   │ features and 1 categorical. Analysis shows...     │  │
│   └────────────────────────────────────────────────────┘  │
│                                                            │
│   ┌────────────────────────────────────────────────────┐  │
│   │          DATA VISUALIZATION                       │  │
│   │                                                    │  │
│   │           [Chart Image Display]                   │  │
│   │                                                    │  │
│   └────────────────────────────────────────────────────┘  │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

---

## 🔌 API Endpoints

```
Express API (Port 3000)
│
├── GET /health
│   └─ Response: { "status": "ok" }
│
└── POST /api/analyze
    ├─ Request: { "datasetUrl": "https://..." }
    │
    └─ Response:
       {
         "summary": {
           "rows": 150,
           "columns": 5,
           "null_counts": {...},
           "column_types": {...}
         },
         "metrics": {
           "numeric": {...},
           "categorical": {...}
         },
         "chat": "AI generated insights...",
         "chart": "base64_encoded_png"
       }
```

---

## 🎯 Component Props Flow

```
App
├─ QueryInput
│  ├─ Props: onQuery(url), isLoading
│  └─ State: url, loading
│
├─ Dashboard
│  ├─ Props: data, datasetUrl
│  ├─ Sub: SummaryCard
│  │       ├─ Props: summary
│  │       └─ Renders: 4 StatCards
│  │
│  ├─ Sub: MetricsCard
│  │       ├─ Props: metrics
│  │       └─ Renders: Multiple StatCards
│  │
│  └─ Sub: Display Chat & Chart
│
└─ Error Display (Conditional)
```

---

## 🚀 Service Startup Sequence

```
1. User runs start.bat or start.sh
   │
   ├─ Terminal 1: cd python_agent && python app.py
   │              └─ Flask starts on Port 8001
   │                 (20-30 seconds)
   │
   ├─ Terminal 2: node api-server.js
   │              └─ Express starts on Port 3000
   │                 (2-3 seconds)
   │
   └─ Terminal 3: cd Frontend && npm run dev
                  └─ Vite starts on Port 5173
                     (3-5 seconds)

2. User opens browser: http://localhost:5173

3. Frontend loads and shows Query Input

4. User enters CSV URL and clicks Analyze

5. Request flows through system and returns results
```

---

## 📊 Data Transformation Pipeline

```
Raw CSV File
    │
    ▼
Pandas DataFrame
    │
    ├─ Shape Analysis → rows, columns
    │
    ├─ Dtype Inference → column_types
    │
    ├─ Null Detection → null_counts
    │
    ├─ Describe Stats → metrics
    │
    ├─ Chart Gen → base64 image
    │
    └─ AI Analysis → insights text
         │
         ▼
    JSON Response
         │
         ▼
    Frontend Display
```

---

## 🎬 Animation Timeline

```
Page Load (0s)
    │
    ├─ 0.0s: Background orbs animate (infinite)
    │
    ├─ 0.3s: Header fades in
    │
    ├─ 0.5s: Search bar slides up
    │
    └─ 0.7s: Example info fades in

User Submits (Click Analyze)
    │
    ├─ 0.0s: Loading spinner appears (rotating)
    │
    └─ 3-5s: Results received

Results Display (Response Received)
    │
    ├─ 0.3s: Dashboard container fades in
    │
    ├─ 0.4s: Summary card slides up
    │
    ├─ 0.5s: Metrics card slides up
    │
    ├─ 0.6s: Chat card slides up
    │
    └─ 0.7s: Chart card slides up
```

---

## 🔒 Request/Response Flow with Error Handling

```
Request
    │
    ▼
Validation ──No──> Error Response
    │
   Yes
    │
    ▼
API Gateway
    │
    ├─ Try ─ Parallel Requests ─ Success ──> Transform
    │                                           │
    │                                          ▼
    │                                      Response (200)
    │
    └─ Error ──────────────────────────> Error Response (500)
         │
         └─ Log & Fallback
```

---

## 💾 Database Integration (Optional)

```
Flask Backend
    │
    ├─ Analyze Dataset
    │
    ├─ Store Results (Optional MongoDB)
    │  └─ collections: analyses
    │     documents: {
    │       url: string,
    │       timestamp: date,
    │       summary: object,
    │       metrics: object,
    │       chat: string,
    │       chart: string
    │     }
    │
    └─ Retrieve for History
```

---

This architecture provides a clean separation of concerns, scalability, and professional-grade error handling! 🚀
